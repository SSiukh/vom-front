import { Routes } from '@angular/router';
import { StickerGenerator } from './pages/sticker-generator/sticker-generator';

export const STICKER_ROUTES: Routes = [{ path: '', component: StickerGenerator }];
